# npm start (React dev server, usually http://localhost:3000)
REACT_APP_API_URL=http://localhost:5000

# --- Docker frontend only (cd alzheimer-app && docker compose up --build) ---
# DOCKERHUB_USER=local
# IMAGE_TAG=latest
# FRONTEND_PORT=8080
# REACT_APP_API_URL=http://localhost:5000
# NGINX_CONF=nginx.standalone.conf
