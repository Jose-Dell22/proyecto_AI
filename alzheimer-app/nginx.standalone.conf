# Frontend-only image: static SPA. API calls use REACT_APP_API_URL baked at build time.
server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  client_max_body_size 12M;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
